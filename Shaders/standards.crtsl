surface constant(color4 surfColor = color4(1, 1, 1);)
{
	out = surfColor;
}

surface matte(	color4 surfColor = color4(1, 1, 1);
				real Ka = 1;
				real Kd = 1;)
{
	vec3 Nf = faceforward(N, I);
	out = surfColor * (Kd * diffuse(Nf));
}